<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse ">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#nav2">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if current_user && current_user.user? %>
        <%= link_to 'Moje mieszkania', my_apartments_path, class: 'navbar-brand' %>
      <% elsif current_user && current_user.admin? %>
        <%= link_to 'Użytkownicy', users_path, class: 'navbar-brand' %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse" id="nav2">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_user_links' %>
      </ul>
    </div>
  </div>
</nav>
